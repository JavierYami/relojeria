const initialState = {
    // tu estado inicial
  };
  
  function reducer(state = initialState, action) {
    switch (action.type) {
      // tus casos de acción
      default:
        return state;
    }
  }
  
  export default reducer;